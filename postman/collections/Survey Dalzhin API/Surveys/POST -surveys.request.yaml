$kind: http-request
name: POST /surveys
url: "{{baseUrl}}/api/surveys"
method: POST
headers:
  Authorization: Bearer {{token}}
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "name": "My Survey",
      "description": "Test",
      "questions": [
        {
          "text": "Question?",
          "typeId": 1,
          "order": 1,
          "options": []
        }
      ]
    }
order: 2000
